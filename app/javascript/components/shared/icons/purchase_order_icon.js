import PurchaseOrderIcon from "@material-ui/icons/Ballot";
import {styled} from "@material-ui/core/styles";

const FlippedIcon = styled(PurchaseOrderIcon)({
  transform: "scaleX(-1)",
});
export default FlippedIcon;
