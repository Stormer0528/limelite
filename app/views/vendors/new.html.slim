= content_for :page_title
  '&nbsp; :: Vendors > New

- content_for :header_content do
  h2.page-header
    = class_icon "vendor", icon_options: "medium"
    span.text
      = link_to vendors_path, class: "breadcrumb primary" do
        span.link-text
          '  Vendors
      '
      span.breadcrumb New

== render partial: 'form', locals: { back_path: vendors_path }
